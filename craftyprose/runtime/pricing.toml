# CraftyProse pricing table (ADR-016). USD per million tokens.
#
# Source: Anthropic first-party API rates as cached in the claude-api reference
# on 2026-06-24. Cache writes are 1.25x input for the 5-minute TTL and 2x for
# 1-hour; cache reads are 0.1x input unless a model lists its own rate.
# Verify against the published pricing page before relying on totals.
#
# A model or server tool that is not listed here is reported as "unpriced" by
# the meter, never costed as zero.

version = "2026-06-24"
source = "Anthropic API pricing via claude-api reference (cached 2026-06-24)"

[models."claude-opus-5"]
input = 5.00
output = 25.00
cache_read = 0.50
cache_write_5m = 6.25
cache_write_1h = 10.00

[models."claude-opus-5-5"]
input = 4.00
output = 20.00
cache_read = 0.20
cache_write_5m = 5.00
cache_write_1h = 8.00

[models."claude-sonnet-5"]
input = 2.00
output = 10.00
cache_read = 0.20
cache_write_5m = 2.50
cache_write_1h = 4.00

[models."claude-haiku-4-5"]
input = 1.00
output = 5.00
cache_read = 0.10
cache_write_5m = 1.25
cache_write_1h = 2.00

[models."claude-fable-5-1"]
input = 10.00
output = 50.00
cache_read = 0.25
cache_write_5m = 12.50
cache_write_1h = 20.00

# USD per use. Web search and web fetch fees are not in the cached reference,
# so they stay unpriced until confirmed from the pricing page.
[server_tools]
